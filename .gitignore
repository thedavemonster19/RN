node_modules/
dist/
dist-ssr/
*.local
.DS_Store

# Env / secrets
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.claude/settings.local.json

# Unrelated personal files that predate this project. This repo is PUBLIC
# (GitHub Pages serves /docs), so nothing in here should ever be pushed.
Archive/

# Capacitor / iOS build artifacts (added later)
ios/App/Pods/
ios/App/build/
ios/App/App/public/

# Master artwork source. The processed sprite sheet is baked into
# src/data/foodSheet.ts, so the repo does not need the original too.
art/
